## Version 3.0.1

- Add `TRALICSCONFDIR` environment variable, to specify the location of `tralics.conf`.
- Add the `thesis` bibtex type.
- Parse the `date` field in `.bib` files, and export it as is.